{
  "name": "prerender-core",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic build-time prerender engine: crawls a fetch-shaped server handler and writes static HTML. Internal to @solidjs/vite-plugin-prerender for now; deliberately free of Solid and Vite imports so it can be published standalone when a consumer outside the Solid ecosystem materializes.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^4.0.0"
  }
}
